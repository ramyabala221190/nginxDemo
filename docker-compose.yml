services:
   nginx-demo:
     container_name: testing-nginx-container
     image: testing-nginx
     env_file: 
       - ./$${environment}.env
     build:
       context: .
       dockerfile: Dockerfile
     ports:
       - 8081:80